import 'package:flutter/material.dart';

/// Premium Notes App Color Palette
/// Glassmorphism meets Material 3 design
class NotesAppColors {
  // Primary Brand Colors
  static const primary = Color(0xFF6B4CE6);
  static const primaryLight = Color(0xFF9D7FF5);
  static const primaryDark = Color(0xFF4A2FB8);

  // Secondary Accent
  static const secondary = Color(0xFFFF6B9D);
  static const secondaryLight = Color(0xFFFF9FBF);
  static const secondaryDark = Color(0xFFE63F7A);

  // Background Colors
  static const backgroundLight = Color(0xFFF8F9FE);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const cardLight = Color(0xFFFDFDFD);

  static const backgroundDark = Color(0xFF0F0F1E);
  static const surfaceDark = Color(0xFF1A1A2E);
  static const cardDark = Color(0xFF242438);

  // Text Colors
  static const textPrimaryLight = Color(0xFF1A1A2E);
  static const textSecondaryLight = Color(0xFF6B7280);
  static const textTertiaryLight = Color(0xFF9CA3AF);

  static const textPrimaryDark = Color(0xFFFFFFFF);
  static const textSecondaryDark = Color(0xFFD1D5DB);
  static const textTertiaryDark = Color(0xFF9CA3AF);

  // Note Card Colors - Pastel Palette
  static const List<Color> noteCardColorsLight = [
    Color(0xFFFFE5E5), // Soft Pink
    Color(0xFFFFEBCC), // Soft Orange
    Color(0xFFFFF9CC), // Soft Yellow
    Color(0xFFD4F1D4), // Soft Green
    Color(0xFFD4E8FF), // Soft Blue
    Color(0xFFE8D4FF), // Soft Purple
    Color(0xFFFFD4E8), // Soft Rose
    Color(0xFFD4FFF4), // Soft Mint
  ];

  static const List<Color> noteCardColorsDark = [
    Color(0xFF3D2E2E), // Dark Pink
    Color(0xFF3D352E), // Dark Orange
    Color(0xFF3D3B2E), // Dark Yellow
    Color(0xFF2E3D2E), // Dark Green
    Color(0xFF2E353D), // Dark Blue
    Color(0xFF352E3D), // Dark Purple
    Color(0xFF3D2E35), // Dark Rose
    Color(0xFF2E3D3A), // Dark Mint
  ];

  // Status Colors
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Glassmorphism Effects
  static const glassSurfaceLight = Color(0xF2FFFFFF);
  static const glassSurfaceDark = Color(0xF21A1A2E);

  // Shadows
  static const shadowLight = Color(0x1A000000);
  static const shadowDark = Color(0x40000000);

  // Dividers
  static const dividerLight = Color(0xFFE5E7EB);
  static const dividerDark = Color(0xFF374151);
}
